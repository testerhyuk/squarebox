spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sb_data?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    username: squarebox
    password: squarebox
    driver-class-name: com.mysql.cj.jdbc.Driver

jpa:
  hibernate:
    ddl-auto: create
  properties:
    hibernate:
      format_sql: true

logging:
  level:
    org.hibernate.SQL: debug